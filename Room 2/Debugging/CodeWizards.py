#Fix this code so it returns a reversed copy of the list without modifying the original one.
def reverse_list(lst):
    return lst.reversed()

print(reverse_list([1, 2, 3])) #Expected Output: "3, 2, 1"